---
description: Go 项目禁止使用 fmt 直接输出，统一使用全局一个 log 对象
globs: **/*.go
---

# Go 日志与输出规范

- **禁止**使用 `fmt.Printf`、`fmt.Println`、`fmt.Fprintf`、`fmt.Print` 等直接写 stdout/stderr。
- **必须**统一使用**全局一个 log 对象**完成所有输出（包括正常程序输出与错误/日志）：
  - 使用标准库时：全项目只用 `log` 包（`log.Printf`、`log.Println`、`log.Fatalf` 等），不要混用 `fmt` 做输出。
  - 使用自定义 logger 时：在合适位置（如 `main` 包或 `internal/logger`）定义**唯一**的包级变量（如 `var Log = log.New(...)`），全项目仅通过该变量输出，不直接使用 `fmt` 或零散的 `log.New`。
- **禁止**使用 `fmt.Fprintf(os.Stderr, ...)` 输出错误或日志；一律走上述全局 log。
- 需要退出时用 `log.Fatalf(...)` 或等价调用，其它情况用 `log.Printf` / `log.Println` 或自定义 logger 的对应方法。
