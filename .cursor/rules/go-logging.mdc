---
description: Go 项目禁止使用 fmt.Fprintf 写错误/日志，统一使用 log 包
globs: **/*.go
---

# Go 日志与错误输出

- **禁止**使用 `fmt.Fprintf(os.Stderr, ...)` 输出错误或日志。
- **必须**使用标准库 `log` 包代替：
  - 错误信息：`log.Printf(...)` 或 `log.Fatalf(...)`（需退出时）
  - 默认 `log` 输出到 stderr，无需显式指定 `os.Stderr`。
- 正常程序输出（stdout）仍可使用 `fmt.Printf` 等。
